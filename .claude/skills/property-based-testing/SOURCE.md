Vendored from https://github.com/trailofbits/skills (plugins/property-based-testing/skills/property-based-testing), commit 7be90d6, CC-BY-SA-4.0 (LICENSE) — attribution: Trail of Bits.
Dropped: agents/openai.yaml, assets/ (logo), plugin evals. Content unmodified.
Primary users: tester (write/judge property tests: roundtrip, idempotence, invariant, oracle), reviewer (are existing property tests asserting anything real). Adding a PBT library to a project without one is the user's call — the skill says so itself.
