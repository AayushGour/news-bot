# senior-dev log

- 2026-09-06 [T20] built scripts/eval_compose.py (mechanical deck scoring, no model judge) + stages/examples.py (few-shot decks behind FEW_SHOT_EXAMPLES, default off), → eval
- 2026-09-06 [T21] first baseline invalid: OPENROUTER_MODEL_GOOD was `openrouter/free` (router alias, backing model varies per call) + local fallback is a 2-bit IQ2_M creative tune. Killed run; operator pinned `nvidia/nemotron-3-ultra-550b-a55b:free` (verified real, 1M ctx, text-only); service restarted, baseline rerunning
- 2026-09-06 [T22] host disk 99% full (9.3Gi free of 926Gi); old pipeline process died with sqlite3.OperationalError "database or disk is full". Project owns 137M data + 315M venv + 36G ollama — not the bulk. Flagged to operator, no deletion performed
- 2026-09-06 [T21] baseline (nemotron pinned) vs fewshot, 16 items each: news decks near-identical (slides -0.3, types -0.2, rich +0.6, overlong 2->0, themes 3->4); timing tied (median 41.2s vs 39.8s). bad_repo_urls +8 traced to item 26 notes predating e7ed4ca (no url/owner/logo keys), NOT model fabrication — _restore_repo_facts is correct, data was stale
- 2026-09-06 [T23] fixed two scorer bugs in scripts/eval_compose.py: empty tail passed shape check vacuously; known-URL set ignored note["sources"]. Added intent-aware enumeration_ok + per-run deck persistence. Rescore: enumerations_ok baseline 0/1 vs fewshot 1/1; avg_shape 3.00 -> 2.80 both runs, exposing 5 decks with no closing slide
- 2026-09-06 [T24] closing slide now guaranteed in code (compose.ensure_closing_slide) instead of only requested in the prompt; sources for channel items, follow for DMs or when research yielded no urls; trims last body slide when deck is at MAX_SLIDES. 6 new tests + 2 existing updated to the new contract; 314 pass; 4 mutations (no-op guarantee / ignore is_dm / drop trim / drop url cap) all go red, reverted clean → tester
- 2026-09-06 [T25] requeued item 26; re-research produced 8 notes with full url/logo/owner/stars/language keys. First recompose still lacked a closing slide (service was on pre-T24 code); restarted service, requeued to synthesized, deck now hook + 8x repo + follow = 10 slides
- 2026-09-06 [T26] local fallback chain: MODEL_*_FALLBACK env + LLMClient._local_chain; primary local tried first, spare only if it raises Retryforever or BadCompletion. 6 tests, 320 pass, 3 mutations red
- 2026-09-06 [T27] scripts/dashboard.py mission control (aiohttp, loopback-only): item table, detail with slides/research/brief, approve/reject/drop/requeue, log tail. Path traversal blocked (plain + percent-encoded verified 404). requeue logic moved to src/pipeline/requeue.py so CLI + dashboard + bot share one implementation
- 2026-09-06 [T22] disk resolved on its own: 99%/9.3Gi free at first check, now 77%/211Gi. Added scripts/cleanup.py — deletes only terminal-item media behind --apply, reports Docker/Ollama read-only with a SearXNG warning
- 2026-09-06 [T29] fixed requeue stage table: a status names finished work and the worker keys the NEXT stage off it, so requeue to EXTRACTED must clear triage output, not `extracted` (would have triaged image-only posts blind). Added missing INGESTED + APPROVED entries. tests/test_requeue.py 8 tests, 3 mutations red
- 2026-09-06 [T30] fixed raw-tuple reply_markup in bot.handle_pending_reply caption branch (bot.py:253) — live ValidationError on every caption edit. to_markup was tested in isolation while the caller was not; existing caption tests passed no bot so `if bot:` was false and the strict FakeBot never saw the send. Added 2 caller-level tests, mutation restoring the bug goes red. 330 pass
- 2026-09-06 [T31] item 33 caption clobbered: pending caption prompt swallowed the operator's next message ("Research about panpsychism") as caption text. Original caption unrecoverable — events table stores status transitions only. Queued the request properly as item 34
- 2026-09-06 [T32] enumeration had no relevance gate: score_candidate measures repo quality only, so a GitHub search for "panpsychism" kept popular unrelated repos at confidence 96 and composed a developer-tools deck. Added subject_terms/is_relevant, filtering before scoring; terms come from item.answer when the operator corrected us. 9 tests, 339 pass. First mutation pass caught my own gap: disabling the gate left all tests green because they only exercised the helper — added end-to-end tests, mutation now red
- 2026-09-06 [T33] research: automated content generation workflows, via SearXNG → .claude/research/2026-09-06-automated-content-workflows.md. Key finding: SearXNG general engines degraded (generic-word queries return dictionary entries), which caps the pipeline's own research quality since search.py uses the same backend
- 2026-09-06 [T34] follow slide missing on every channel post: ensure_closing_slide required only that SOME closing type existed, so a deck already carrying "sources" satisfied it and shipped with no call to action. Split into two guarantees — attribution (sources/links, channel only) and follow (always last). Also moved the MIN_SLIDES check before padding, since two generated slides could otherwise lift a one-slide deck to the minimum. 8 tests rewritten, 339 pass, 4 mutations red
- 2026-09-06 [T28] free models only, enforced in code. All three OPENROUTER_MODEL_* defaults were paid Gemini models, so a commented-out override silently billed every triage call (verified against the pricing API: gemini-2.5-flash-lite prompt=1e-7 completion=4e-7). Defaults now free; validate_for_run refuses to start on any non-free slug and names every offender; preflight verifies actual price from the models API rather than trusting the ':free' suffix. 13 tests, 352 pass, 4 mutations red
- 2026-09-07 [T35] pinned eval set: scripts/build_golden.py snapshots 24 real items across 9 failure modes into data/eval/golden.json, self-contained so DB changes cannot drift it. eval_compose.py gains --golden, a prompt fingerprint (sha256 of SYSTEM + examples), and a per-failure-mode breakdown
- 2026-09-07 [T36] baseline vs fewshot on the pinned set, same model both arms (nemotron-3-super-120b-a12b:free; the 550b was ~113s for 5 tokens). Mixed: rich_slides 2.95->4.00, overlong 3->1, enumeration composed 2/4->4/4 and produced the first links slide; but failures 2->3, moving from the list path to news (10/10->8/10). Not conclusive at n=24x1 — the duplicate pair gave 4/5/5/6 slides for one request. Few-shot stays default-off
- 2026-09-07 [T37] hashtag cap counted only appended tags; composer also writes them into the prose and Instagram counts those. 2 of 22 baseline captions had 6-7 tags and would have been rejected at publish. build_caption now lifts inline tags into the same capped set, ordered first; C# and #1 excluded. 6 tests, 358 pass, 3 mutations red
- 2026-09-07 [T41] FEW_SHOT_EXAMPLES is now a scope (off|list|all), not a bool; legacy true/false still parse. Third arm on the golden set: list-only composed 24/24 with 0 failures vs baseline 2 and few-shot 3, and is the only arm 100% on every failure mode. For 20 of 24 cases it sends a byte-identical prompt to baseline, so the result reduces to enumeration 2/4 -> 4/4; across both example-using arms that is 8/8. Mutation caught my own gap again — compose could ignore the scope with all 117 tests green (to_markup, is_relevant, now this: third time testing a helper and not its caller). 386 pass
- 2026-09-07 [T43] clause coverage gate: PLAN_SCHEMA gains `clauses` (decompose a compound request into its asks) and the planner is told every clause needs a query on its own terms — item 45's four queries all welded "AI" onto the clinical half, so nothing searched it. After research, each clause must have support from a non-low-confidence note or the item asks the operator (NeedsInput, resume TRIAGED). New `clauses` column + migration
- 2026-09-07 [T44] compose critique: after the deck is built it is judged against the same clauses and recomposed once with the misses named. Bounded to one retry — research already cleared the clauses, so a third pass on the same brief finds nothing new. Shared judge in src/pipeline/coverage.py, fails open on model error
- 2026-09-07 [T45] wrote caller-level tests first this time. Of 6 mutations, 2 initially "survived" — both were bad seds that never applied (single vs double quotes). Fixing the seds showed one real gap: low-confidence exclusion was only tested via the fallback path. 408 pass
- 2026-09-07 [T46] checked the last run's search terms: they were fine ('early signs of burnout in IT professionals', 'depression symptoms in technology workers'). The bug was the index — intent==list was hardwired to engines=github, which indexes no clinical material, so good queries returned 0. Corrects my earlier "the classifier is wrong" call: its reading was defensible, enumeration just only knew how to find repos
- 2026-09-07 [T47] added catalogue (repos|web) to the intent classifier and threaded it through search engines, scoring, dedupe and note shape. A Mayo page scores 25 as a repo (below the 35 bar) and 68 as web — scoring alone would have emptied the set one stage after the index fix
- 2026-09-07 [T48] query expansion (src/pipeline/expand.py) before searching, and search_many() runs the widened set in parallel; enumeration was strictly serial. Expansion fails open, and Retryforever still propagates so an outage is not flattened into no-results. 432 pass
- 2026-09-07 [T49] mutation harness gave false signal three ways today: seds that never applied (quote mismatch), multi-line args mangled through shell, and stale __pycache__ surviving a revert. Each read as "well tested". Worth a real mutation script
- 2026-09-07 [T53] scripts/mutate.py — 23 mutations, each asserting it applied, reverting byte-identical and clearing bytecode. Immediately earned itself: reported ANCHOR NOT FOUND on call-deadline instead of a false pass
- 2026-09-07 [T40] CALL_DEADLINE_S wraps both transports in asyncio.wait_for. httpx timeout is per-read (gap between bytes), so a trickling response had no wall-clock cap — one compose ran 983s against a 900s "timeout"
- 2026-09-07 [T38] eval records the raw completion on failure via a Recording wrapper; failures were the one case with no saved output
- 2026-09-07 [T51] _score_web saturated at 76 for every decent article, so ranking was search order. Finer gradients + host authority: NIH 75, Forbes 64, blog 45, content farm 10
- 2026-09-07 [T39] could NOT reproduce the leniency I claimed — stored text shows the gate rejecting both junk repos, so that claim was unproven. Real defect found instead: raw substring matching let "discontent" satisfy "content". Now token-level with negating prefixes excluded, which keeps pgvector matching "vector"
- 2026-09-07 [T50] web enumerations extract THINGS from the documents instead of listing the documents; falls back to source-listing if extraction returns nothing
- 2026-09-07 [T52] clause gate now runs on the enumeration path too, so both routes ask rather than answering half a request
- 2026-09-07 [T42] links index guaranteed in code — both example arms produced one in only 1 enumeration of 4
- 2026-09-07 [T31] a pending caption prompt no longer swallows a new request; ambiguous replies go back to the operator with a "caption:" escape hatch
- 2026-09-07 [T36] FEW_SHOT_EXAMPLES defaults to "list" on the golden-set evidence (24/24, 0 failures vs 22 and 21); byte-identical prompt to off on news
- 2026-09-07 [T55] quality check vs golden_listonly (same prompt fingerprint, same model). On the 19 decks that composed: slides 6.4->6.5, distinct types 5.21->5.47, rich 3.38->3.53, empty 0, shape 3.00, enumerations_ok 1/4 -> 3/3. Quality on par or better
- 2026-09-07 [T55] the check caught a regression I had shipped: ensure_links_slide put the index at the end of a full deck and ensure_closing_slide then trimmed from the end to fit `follow`, deleting it. Both correct alone, both individually tested, 468 tests green — T42 was silently doing nothing. Trim now comes out of the body; closing slides ordered unconditionally
- 2026-09-07 [T55] 5 failures, none attributable to the changes: 3 openrouter 404 "Provider returned error", 2 model degeneration (item 33 emitted the identical compare slide 9x with no rows; item 3 emitted compare slides with no rows). News items get a byte-identical prompt under FEW_SHOT_EXAMPLES=list, and both slide guarantees run after the model returns, so neither can affect what it emits. Diagnosed entirely from the completions T38 now saves
- 2026-09-08 [T58] answers now route by swipe-reply. handle_answer took list_by_status(limit=1) — ORDER BY id — so with two items waiting the lower id consumed every reply whatever question it answered, and the other stayed parked. ask() records question_msg_id; routing is reply-to, then an "45:" id prefix, then single-waiting; anything else lists the waiting items and asks rather than guessing. 8 tests, 3 mutations red, 478 pass. FakeBot now returns a distinct message_id per send — a constant would have made reply-routing look like it worked
- 2026-09-11 [T59] dashboard rewritten: jobs grouped into waiting-on-you / running / done / stopped, per-item timeline from events, per-item log lines grepped from pipeline.log, rendered slides, and a chat per item. Added a messages table so Telegram and the dashboard share ONE conversation per item — a question asked in the bot can be answered in the browser and vice versa. ask()/handle_answer() now record both sides. Sending on a needs_input item answers it and resumes; on awaiting_approval it becomes a regen note and recomposes; otherwise it is recorded and changes nothing. Loopback-only, traversal still 404s. 485 pass
- 2026-09-12 [T60] removed every content cap. Biggest was search.MAX_TEXT_CHARS=6000 — every fetched article was cut to 6k before any model saw it. Also removed: triage doc [:1500], research doc [:2000], extraction body [:2500], synthesize note detail [:120], research/enumerate note detail [:160], coverage material [:220] and 40-item cap, compose image description [:180], the question preview [:120], digest [:300]/[:600], and all dashboard display caps + log tail/line caps. 492 pass, 31 mutations caught
- 2026-09-12 [T60] NOT removed, with reasons: MAX_SLIDES=10 and MAX_HASHTAGS=5 are Instagram's and removing them means the API rejects the post; MAX_LINKS/MAX_SOURCE_URLS/code[:900] are what the slide template can physically fit and the render overflow guard would reject anyway; Telegram's 4096/1024 are not ours to remove; error strings stay bounded at 2000 in the DB so a runaway traceback cannot fill the column
- 2026-09-12 [T61] RISK: local fallback runs at num_ctx_good=16384. Uncapped documents will overflow that silently on the local model — OpenRouter models are 262k-1M so the hosted path is fine. Raise NUM_CTX_GOOD before relying on the local chain with long sources
- 2026-09-12 [T62] clause gate withdrawn from channel items. A news post asks for nothing, so the planner split its own prose — item 88 was parked demanding a source for "r considerably harder to dismiss as pure speculation", a mid-word slice of the article's last sentence. All 19 stalled items were parked this way; the gate now applies to dm requests only. Requeued the 19; item 58 (a genuine operator request) left parked
- 2026-09-12 [T63] ask() recorded the question AFTER sending, so a TelegramNetworkError lost it from the thread and escaped _run_one, killing the worker pass. Now recorded first and the send is wrapped: the item is parked and the question stored, so delivery failure is survivable and visible in the dashboard
- 2026-09-12 [T64] NeedsInput carries the work already done (research, clauses) and ask() persists it, so answering resumes from searches already paid for and a parked item can be inspected. Previously clauses came back empty from the database on every parked item
- 2026-09-12 [T62-64] 498 pass, 4 new mutations caught (gate scope, record-before-send, outage survivability, carried work)
- 2026-09-13 [T65] new-post composer on the dashboard. Queue logic factored into src/pipeline/intake/manual.py so the dashboard and scripts/queue_item.py share one implementation — the negative-id rule (hand-queued items take ids below zero so they cannot collide with a Telegram message the listener backfills later) is exactly the detail a second copy would drift on. Same MIN_REQUEST_CHARS floor as DM intake; a too-thin request returns to the board with the reason instead of an error page. 509 pass, 2 new mutations caught
- 2026-09-13 [T66] dashboard redesigned. Navigation: lane tabs with live counts, text search, "#91" jumps straight to the item, prev/next between items, 25 rows with show-all, auto-refresh only while something is mid-flight. Presentation: slide thumbnail per row, relative timestamps, status dot + badges, collapsible timeline/log/research/brief, proper type scale and spacing, light and dark. Found and fixed a latent bug on the way: status_updated_at was a real column missing from the Item dataclass, so reading it off an Item rather than out of SQL raised AttributeError — the detail page 500'd. 510 pass
- 2026-09-13 [T67] priority column. claim_items was strictly ORDER BY id, so an urgent request sat behind every older one; now ORDER BY priority DESC, id with DEFAULT 0, so an untouched queue is byte-for-byte the old behaviour and one bumped item jumps the line without reordering anything else. Dashboard gets per-row up/down controls plus buttons on the detail page; priority is shown only when non-zero. Verified live: [85,86,88,89,91,92] -> [89(+2),86,88,91,92,85(-1)]. 517 pass, 3 mutations caught including the two interactions worth pinning — priority must not override backoff (a bumped failing item would retry in a tight loop) and must not resurrect an item halted for approval
- 2026-09-13 [T68] publishing live end to end. MinIO (docker, quay.io — Docker Hub refused the pull) + cloudflared quick tunnel; a real 1080x1350 slide fetched publicly at 94KB before anything was switched on. DRY_RUN=false, .env backed up first
- 2026-09-13 [T69] approvals never ran: tick() gathers its whole batch before it can claim again, so one slow compose (983s observed) blocked every approved item. Split into run_slow (research) and run_fast (APPROVED/PUBLISHING, 2s poll) — disjoint status sets, so no lease needed. 3 tests including one that holds a research stage open and asserts publishing still goes
- 2026-09-13 [T70] both posts then failed at media_publish with 400 "Media ID is not available", one second after creating the carousel. Instagram builds containers asynchronously; Meta documents polling GET /<id>?fields=status_code until FINISHED. Added _await_container. Two real posts lost to this
- 2026-09-13 [T71] my own poll loop had an infinite-loop bug: bound was accumulated sleep, so a zero interval never advanced it. Bounded by attempts instead
- 2026-09-13 [T72] mutate.py left `if False:` in instagram.py after I pkill'd it — SIGTERM skips finally. Now writes an inflight sidecar and restores any leftover on next start. Cost an hour chasing a test failure that was my own leftover mutation
- 2026-09-13 [T73] requeue did not clear ig_child_ids/ig_carousel_id. A container has its caption and image URLs baked in at creation, so recomposing and republishing would silently post the PREVIOUS deck. Added PUBLISHING to the requeue table
